import { Template } from '@/layout';

export default function UsersPage() {
  return (
    <Template title="Orders">
      <div className="h-[400px] w-full bg-slate-300">users content</div>
    </Template>
  );
}
