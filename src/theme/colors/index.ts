import * as colors from "./colors";

const pallets = {
  light: {
    primary: colors.purple,
    secondary: colors.blue,
    text: colors.text,
  },
  dark: {
    primary: colors.purple,
    secondary: colors.blue,
    text: colors.text,
  },
};

export * from "./colors";
export default pallets;
